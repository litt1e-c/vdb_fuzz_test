operator_id: power
family: arithmetic
syntax_examples:
  - 'price ** 2 > 1000'
  - 'c_int ** 2 == 25'
  - 'c_double ** 3 < 0'

doc_status: operator_documented
doc_source_urls:
  - https://milvus.io/docs/id/basic-operators.md
doc_supported_types:
  - scalar_numeric
doc_semantics: "The checked official Basic Operators page lists `**` as a supported arithmetic operator and gives `price ** 2 > 1000` as an example filter expression."
doc_notes:
  - "The checked official page documents `**` with a field-based example but does not mention any restriction that power applies only to constants."
  - "The checked official page does not define overflow, exponent domain restrictions, or type-specific behavior for `**`."

verify_status: unsupported
observed_summary: "On the local 19532 deployment, all tested field-based exponentiation forms were rejected for INT, FLOAT, and DOUBLE fields. Expressions such as `c_int ** 2 == 25`, `c_float ** 2 > 20.0`, and `c_double ** 3 == 0.0` failed with the parser error `power can only apply on constants`."
oracle_support: []
case_refs: []
repro_path: /home/caihao/compare_test/operator_test/milvus/power_operator.py

oracle_mapping: "The current fuzzer does not emit `**` expressions because local validation on 19532 shows documented `field ** const` forms are rejected with a parser error, so no pandas/PQS mapping is claimed for this operator yet. This is our oracle assumption, not an official claim."
