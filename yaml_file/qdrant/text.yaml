operator_id: text
family: match_condition
syntax_examples:
  - '{"key":"description","match":{"text":"brown"}}'
  - '{"key":"title","match":{"text":"quick brown"}}'
  - '{"key":"title","match":{"text":"needle"}}'

doc_status: operator_documented
doc_source_urls:
  - https://qdrant.tech/documentation/concepts/filtering/
  - https://qdrant.tech/documentation/concepts/indexing/
doc_supported_types:
  - string
doc_semantics: "The `text` condition is documented for text search on string payloads; when no full-text index is created, the checked docs describe it as exact substring matching, while with a full-text index the behavior depends on the text index configuration."
doc_notes:
  - "The checked docs make text-search behavior index-sensitive, including tokenizer and normalization settings when a full-text index exists."
  - "The current fuzzer does not create full-text indexes for STRING fields, so indexed token-level semantics are outside current implementation scope."

verify_status: edge_validated
observed_summary: "On local Qdrant v1.17.0 with qdrant-client v1.17.0, both REST and gRPC matched exact substring behavior for `MatchText` on present scalar string values without a full-text index, including a non-contiguous multi-word negative case; missing/null and the tested non-string row did not satisfy ordinary `text` predicates."
oracle_support:
  - scalar STRING MatchText without full-text index
  - MatchText generation in the main oracle workflow
  - MatchText generation in the PQS workflow
case_refs: []
repro_path: /home/caihao/compare_test/operator_test/qdrant/text_operator.py

oracle_mapping: "`text` is mapped to pandas/PQS as exact substring containment on present scalar string values, with missing/null and tested non-string rows treated as non-matches; current fuzzer coverage is limited to the no-full-text-index subset on STRING fields because the fuzzer still creates KEYWORD indexes rather than full-text indexes for those fields. This is our oracle assumption, not an official claim."
